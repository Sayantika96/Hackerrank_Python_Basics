# Problem:

Write a Python code that takes two numbers as input and determines which one is greater. 
It should then print a message indicating which number is larger or if they are equal.

# Solution

  def get_user_input(user_input):
    try:
        number = float(input(user_input))
        return number
    except ValueError:
        print ("Invalid input!!")
        
input1 = get_user_input("Enter first number: ")
input2 = get_user_input("Enter second number: ")

if (input1 > input2):
    print("Input 1 is greater")
elif (input1 < input2):
    print("Input 2 is greater")
else:
    print("Both are equal")
