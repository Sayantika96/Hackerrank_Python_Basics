# Problem:

Write a Python code that takes an integer as input and prints the multiplication table for that number from 1 to 10.

Solution:

def gen_mult_table(number, range_limit):
    for i in range(1, range_limit + 1):
        result = number * i
        print(f"{number} * {i} = {result}")
user_number = int(input("Enter a number: "))
user_range_limit = int(input("Enter a range limit: "))
gen_mult_table(user_number, user_range_limit)
